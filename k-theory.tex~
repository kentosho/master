x%#!platex master.tex
\section{トポロジカルK-theory}
\subsection{トポロジカルK-theoryの実際的な定義}
\nocite{AtiyahKTh}
\nocite{IwanamiKisoTopology}

抽象的なK-theoryの定義は後回しにするとして、まずはある空間の上の$K$群(若し
くは$K_0$群)を定義する。
\begin{Def}[空間上の$K$群]
 \label{simple-kgroup}
 空間$X$の上の全てのベクトル束の同型類の集合を$\Vect (X)$と書く。このとき
 空間$X$の$K$群(もしくは$K_0$群)は、次のような同値関係を用いて
 ($E,F,G\in\Vect (X)$とする。)
 \begin{eqnarray}
  (E,F)\sim (E\oplus G,F\oplus G) \\
  K \equiv\Vect (X)\times\Vect (X) /\sim
 \end{eqnarray}
 と定義される。
\end{Def}

ここで幾何学でよく知られた定理がある。
\begin{The}
 あるベクトル束$E$が与えられた時にベクトル束$F$があって、その直積
 $E\oplus F$が自明なベクトル束になる。
 \proof
 Atiyahの教科書\cite[Section1.4]{AtiyahKTh}を参照。\QED
\end{The}
この定理を使うと、次数$n$の自明なベクトル束を$\mathfrak{n}$と表記して多様
体$X$の$K$群は必ず$(\mathfrak{n},G)$の形に書ける。よって、
\begin{equation}
 K(X)\simeq\Intg\times Vect(X)
\end{equation}
が言える。

\subsection{$K$群の抽象的な定義}
次にK群の抽象的な定義を与える。
\begin{Def}[$K$群]
 \label{kgroup} 

 $X$を任意の空間とし、$\Vect (X)$を$X$上のベクトル束の集合に半群の構造を入
 れたものとする。但し、加法はベクトル束の直積によって定義するものとする。

 このとき、任意の半群$\AlgA$に対して以下のような性質をもつアーベル群
 $K(\AlgA)$が唯一つ存在する。この$K(\AlgA)$を半群$\AlgA$の$K$群(若しくは
 $K_0$群)という。
 \begin{enumerate}[K1]
  \item ある半群準同形$\alpha :\AlgA\rightarrow K(\AlgA)$があって\\
        任意の群$G$と半群準同形$\gamma :\AlgA\rightarrow G$に対して、\\
	群準同形$\kappa :K(\AlgA)\rightarrow G$で
	$\gamma =\kappa\alpha$を満たすものが唯一つ存在する。\\
 \end{enumerate}
\end{Def}

$K$群の唯一性を証明する前に、このような$K$群を具体的に構成する。これは最初
に定義した$K$群の定義に一致している。まず、$F(\AlgA)$を$\AlgA$の元すべてか
らなる自由アーベル群とする。$E(\AlgA)$を$a+b-(a\oplus b)\quad(a,b\in
\AlgA)$で生成される$E(\AlgA)$の部分群とする。但し$+,\oplus$はそれぞれ自由
アーベル群の加法と半群$\AlgA$の加法である、この時、
\begin{The}[$K$群の具体形]
 \label{concrete-kgroup}
 $K(\AlgA)\equiv F(\AlgA)\diagup E(\AlgA)$である。
 \proof 定義\ref{kgroup}で定義した$\alpha$として、自明な写像、即ち$a\in
 \AlgA$に対して$a$の同値類$[a]$を対応させるような写像を選ぶ。\\
 今群$G$と半群準同 形$\gamma$を与えたとき、$\tilde{\kappa}$として
 $a\in E(\AlgA)$を $\gamma(a)$に映すような写像を選ぶ。\\
 この$\tilde{\kappa}$は$[a]$の同値類の選び方に依らない。何故なら、
 $a,b,c,d\in \AlgA$として$\gamma,\kappa$が準同形であることから、
 \begin{equation}
  \tilde{\kappa}(a+b+c-(b\oplus c))=\gamma(a)
 \end{equation}
 であるからである。よって、$\tilde{\kappa}$は
 $\kappa:F(\AlgA)\diagup E(\AlgA)\rightarrow G$に持ち上がる。\\
 また、このことは$\kappa$の唯一性も示している。\QED
\end{The}

\noindent\textbf{$K$群の唯一性の証明}\par
\ref{kgroup}の群$G$として$K$群としての性質を満たす$K'(\AlgA)$を考えると準
同形$\kappa :K(\AlgA)\rightarrow K'(\AlgA)$を与える。\\
同様にして準同形$\kappa':K'(\AlgA)\rightarrow K(\AlgA)$を得ることができ
る。\\
$\kappa$の唯一性から、$\kappa'\circ\kappa$は恒等写像でなくてはならない。\\
よって、$K(\AlgA)\equiv K'(\AlgA)$が言える。\QED

さらに、図\ref{functor-fig}は可換であり、半群準同形$\delta
:\AlgA\rightarrow\AlgB$について、次のことが言える。
\begin{figure}[h]
%$$\begin{CD}
%   \AlgA @>{\delta}>> \AlgB @>{\epsilon}>> \AlbC \\
%   @V{\alpha_AlgA}VV @V{\alpha_\AlgB}VV @V{\alpha_\AlgC}VV \\
%   K(\AlgA) @>{K(\delta)}>> K(\AlgB) @>{K(\epsilon)}>> K(\AlgC)
%\end{CD}$$
$$\xymatrix{
 \AlgA \ar[d]^{\alpha_\AlgA} \ar[r]_{\delta}
 & \AlgB \ar[d]^{\alpha_\AlgB} \ar[r]_{\epsilon}
 & \AlgC \ar[d]^{\alpha_\AlgB} \\
 K(\AlgA) \ar[r]^{K(\delta)}
 & K(\AlgA) \ar[r]^{K(\epsilon)}
 & K(\AlgA) \\}$$
 \caption{K-theoryの関手性}
 \label{functor-fig}
\end{figure}
但し$K(\delta)$は半群準同形写像$\alpha_\AlgB\circ\delta$により、
$K(\AlgA)$の定義から定まる準同形写像である。$K(\delta)$の定義より、この図
式は可換である。
\begin{The}[K-theoryの関手性]
 $(\alpha,K(\AlgA))$の組は半群の成す圏から、abel圏への関手となっている。即
 ち、定義\ref{functor}のような性質を満たしている。(図\ref{functor-fig})
 \proof
 定義\ref{functor}の$F1$は$K(\AlgA)$の唯一性から直ちに従う。\\
 $F2$も、K群の定義である$\kappa$の唯一性から従う。\QED
\end{The}

\subsection{双対$K$群と$\Kt$群}
\label{relative-kgroup}
$K$群の定義を定義する際に$K_0$群という別称を与えたのは
$K_n(n\in\Intg)$ という$K$群の系列があるからである。この$K_n$は
$\Kt$群と双対$K$群を用いて定義される。

まず以下のような圏の関手を考える。但し、$\CatC$はコンパクト空間の成す圏で
$\CatC^+$はbasepointを定めたコンパクト空間の成す圏で$\phi$は空集合である。
\begin{equation}
 \begin{array}{ccc}
  \CatC^2 &\longrightarrow &\CatC^+ \\
  (X,Y)   &\longmapsto     &X/ Y \\
 \end{array}
 \quad\quad
 \begin{array}{ccc}
  \CatC   &\longrightarrow &\CatC^2 \\
    X     &\longmapsto     &(X,\phi)\\    
 \end{array}
\end{equation}

$\Kt$群は、\ref{relative-kgroup}で定義した$\CatC^+$の対象に対して定義されて
いる。その定義は
\begin{Def}[$\Kt$群]
 空間$X$とそのbasepoint$x_0$について包含写像$i:\{x_0\}\rightarrow X$を定義
 する。そこで、$\Kt$群は$i$の引き戻し写像$i^\ast$について
 \begin{equation}
  \Kt\equiv\Ker(K(X)\xrightarrow{\ast} K(x_0))
 \end{equation}
 と定義される。
\end{Def}
上の定義と準同形定理から$K(X)\simeq\Kt(X)\oplus K(x_0)$がいえる。
$\Kt$は$\CatC^+$からabel圏への関手になっている。

この$\Kt$群を使って、双対$K$群を定義する。
\begin{Def}[双対$K$群]
 \label{relative-ktheory}
 $$ K(X,Y)\equiv\Kt(X/Y) $$
\end{Def}
特に、$K(X,\phi)\simeq K(X)$である。

ここで、smash積を定義する。
\begin{Def}[smash積]
 $X,Y\in\Ob(\CatC^+)$として、
 \begin{equation}
  X\wedge Y\equiv X \times Y / ((X\times y_0)\cup(x_0\times Y))
 \end{equation}
\end{Def}
この積は結合律を満たす。即ち$X,Y,Z\in\Ob(\CatC^+)$として
\begin{equation}
 (X\wedge Y)\wedge Z \simeq X \wedge (Y \wedge Z)
  = X\times Y\times Z / 
  ((X\times Y\times z_0\cup X\times y_0\times Z\cup x_0\times Y\times Z))
\end{equation}

今、$I=[0,1]$として$I/\{0,1\}\in\Ob(\CatC^+)$でありこれは$S^1$に
basepointを定めたものである。この時
\begin{equation}
 S^n=\overbrace{S^1\wedge S^1\wedge\cdots\wedge S^1}^{\hbox{n個}}
\end{equation}
である。

更に、$X\in\Ob(\CatC^+)$として、$S^1\wedge X$を縮約懸垂(reduced
suspension)という。

\subsection{$K_{n}$群}

こうして、$K_{n}$群を定義する準備が整った。
\begin{Def}
\begin{equation}
\begin{array}{lllllr}
 \Kt^{-n}(X)&=&\Kt(S^nX)& & &(K\in\Ob(\CatC^+))\\
 K^{-n}(X,Y)&=&\Kt^n(X/Y)&=&\Kt(S^n(X/Y))&((X,Y)\in\Ob(\CatC^2)) \\
 K^{-n}(X)&=&K^{-n}(X,\phi)&=&\Kt(S^n(X^+))&(X\in\Ob(\CatC))
\end{array}
\end{equation}
\end{Def}
\subsection{Bott周期性定理}

K-theoryで知られている強力な大定理としてBott周期性定理\cite{BottPeriod}がある。
\begin{The}
 \label{Bott-periodicity}
  \begin{equation}
  K(X)\simeq K(X)^{-2}
 \end{equation}
 \proof
 証明は非常に長いので、Atiyahの教科書\cite[Section2.3]{AtiyahKTh}を参照\QED
\end{The}

\section{代数的$K$-theory}

K-theoryには色々な種類があり、幾何的なK-theoryの他に代数的なK-theoryがある。
ここでは、代数的K-$\Cst$環のK-theoryについて説明する。
\subsection{$\Cst$環}

$\Cst$環は、物理の文脈ではしばしば、Hilbert空間に作用するオペレータのなす
環として登場する。非可換幾何学においては、\ref{definition}で述べた通り

$\Cst$環のK-theoryまず最初に代数に対する$*$という操作を定義する。この
*-operationの1例としては、演算子のHermite共役をとるという操作が挙げられる。
\begin{Def}[$*$-Algebra]
 $\Cplx$上の代数$\AlgA$が次の性質をもつような$*:\AlgA\rightarrow\AlgA$を持
 つ時$\AlgA$は$*$-Algebraであると言われる。また$*$の$a\in\AlgA$への作用を
 $a^*$と書く。($a,b\in\AlgA,\,\alpha,\beta\in\AlgA$)として
 \begin{enumerate}[St1]
  \item $a^{**}=a$
  \item $(ab)^*=b^*a^*$
  \item $(\alpha a+\beta b)^*=\bar{\alpha}a^*+\bar{\beta}b^*$
 \end{enumerate}
\end{Def}

