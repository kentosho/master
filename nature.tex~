%#!platex master.tex

非可換場の理論では特有の性質がいくつか知られている。ここではそうした性質を
述べる。

\subsection{基本的な事実}
まずは、非可換場の理論で知られている基本的な事実を羅列する。
\begin{The}
 \label{2nd-order}
 非可換性が一定の場合はラグランジアンの2次の項はMoyal積を普通の積に置き換
 えても、その差は全微分である。
\begin{proof}
 \begin{equation}
  \int d^dx\, \phi_1\ast \phi_2 = \int d^dx\, \phi_1\times \phi_2 
   + \int d^dx\, \theta^{\mu\nu}\partial_\mu(\exp-1)(\ihalf\theta^{\mu\nu}
		   \partial_\mu^y\partial_\nu^z)
		   \phi(y)\partial_\nu^{-1}\phi_2(z)\Big|_{y=z=x}
 \end{equation}
 但し、$\partial_\nu^{-1}$は微分演算子をひとつ取り除くことを表わす。(微分
 演算子がひとつ以上かかっていることは、保証されている。)
\end{proof}

\end{The}

\begin{The}
 2個以上の場のMoyal積をサイクリックに入れ替えても、その差は全微分である。
\begin{proof}
 定理\ref{2nd-order}とMoyal積の結合性より直ちに従う。即ち、
 \begin{eqnarray}
  \int d^dx\, \phi_1 \star \phi_2 \cdots \phi_n  
   &=& \int d^dx\, \phi_1 \star (\phi_2 \cdots \phi_n) \nonumber \\
  &=&  \int d^dx\, (\phi_2 \cdots \phi_n \star \phi_1)
 \end{eqnarray}
\end{proof}
\end{The}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\subsection{UV/IR混合}
\subsubsection{Moyal積の積分型}
\ref{phi4}肢節でも述べたが非可換場の理論にはUV/IR-mixingという現象が起きる。
ここではその現象の物理的起源を述べる。

$\phi_1\star\phi_2$という2つの場のMoyal積の形を変形して次のような表式を
得る。
\begin{equation}
 \label{moyal-integral-form}
 (\phi_1\star\phi_2)(x)=\int d^dx_1 d^dx_2\, \phi_1(x_1)\phi_2(x_2)
  K(x_1,x_2,x)
\end{equation}
ここで積分核$K(x_1,x_2,x)$は次で定義される。
\begin{equation}
 K(x_1,x_2,x)={1\over \det(\theta)}
  \E^{2i(x-x_1)^i\theta^{-1}_{ij}(x-x_2)^j}
\end{equation}
この形は、Moyal積の式(\ref{moyal-product})の$\phi_1,\phi_2$を
Fourier$\rightarrow$逆Fourier変換の形に書き直すことで容易に得られる。

\subsubsection{$\phi_1\star\phi_2$が含む情報}
以下の説明の為には、2次元を考えれば十分であるので、2次元の場合
($x_1^i\rightarrow(x_1,y_1),x_2^i\rightarrow(x_1,y_2)$とする。)を考える。

今、2つのパケット$\phi_1,\phi_2$が2点の相互作用をする場合を考える。2つの
パケットの幅を$(\Delta_{x_1},\Delta_{y_1})$と
$(\Delta_{x_1},\Delta_{y_1})$する。式(\ref{moyal-integral-form})の$x_1$
依存部分を抜き出すと、
\begin{equation}
 \int dx_1\, \phi_1(x_1,y_1)\E^{i\theta^{-1}x_1(y_2-y)}
\end{equation}
となり、パケット$\phi_1$の$x$方向の幅が
\begin{equation}
 \theta^{-1}\Delta_{1x}|y_2-y|\gg 1
\end{equation}
の時はこの相互作用は抑制される。よって、この相互作用によってパケット
$\phi_1$はパケット$\phi_2$の$\delta y_2\approx{\theta\over\Delta_{1x}}$
の範囲にある情報を測ることがわかる。同様にして、
\begin{equation}
 \delta y_2\Delta_{1x}\sim\theta,\quad
 \delta y_1\Delta_{2x}\sim\theta,\quad
 \delta x_2\Delta_{1y}\sim\theta,\quad
 \delta y_1\Delta_{2y}\sim\theta
\end{equation}
が言える。よって、2つのパケットの中心の位置が一致している時、
$(\phi_1\star\phi_2)(x)$ の幅$\Delta_x,\Delta_y$は以下のようになる。
\begin{equation}
 \Delta_x\sim\mathrm{max}(\Delta_{1x},\Delta_{2x},
  {\theta\over\Delta_{1y}},{\theta\over\Delta_{2y}}),\quad
 \Delta_x\sim\mathrm{max}(\Delta_{1y},\Delta_{2y},
  {\theta\over\Delta_{1x}},{\theta\over\Delta_{2x}})
\end{equation}

\subsubsection{$\phi^3$模型のUV/IR混合}
$\phi^3$模型の古典的運動方程式は、
\begin{equation}
 (\partial^2-m^2)\phi={g\over 2}(\phi\star\phi)(x)
\end{equation}
であるから、この古典運動方程式の解のひとつを$\phi_0$とすると、それに対応
する量子論的な解は、$G(x,y)$をこの理論の(2点)グリーン関数とすると、
\begin{equation}
 \phi(x)=\phi_0(x)-{g\over 2} \int d^dx\, G(x-y)(\phi_0\star\phi_0)
  +\cdots
\end{equation}
と摂動論的に書くことができる。最低次のオーダーでは量子効果のソースは、
$-{g\over 2}(\phi_0\star\phi_0)$である。ここで先程の結果を考えると、
$(\phi_0\star\phi_0)$の広がり方は$\mathrm{max}(\Delta,{\theta\over
\Delta})$であるから非常に小さなパケットであってもその相互作用の及ぼす範
囲は非常に大きなものとなる。つまり、非常にエネルギーの低い現象がエネルギー
の高い現象による影響を受けてしまう。よって、non-plannarなダイアグラムに
於いては高エネルギーの仮想粒子に紫外カットオフ$\Lambda$を導入すると、赤
外カットオフ$\theta\over\Lambda$を導入してしまったことにもなってしまう。
これがUV/IR混合と言われる所以である。

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\subsection{弦理論とのアナロジー}

\begin{figure}
 \begin{minipage}[b]{75mm}
  \includegraphics{open-loop.eps}
  \caption{open stringのループ}
  \label{open-loop}
 \end{minipage}
 \begin{minipage}[b]{75mm}
  \includegraphics{closed-loop.eps}
  \caption{closed stringの伝播}
  \label{closed-loop}
 \end{minipage}
\end{figure}
\ref{perturbation}節で見たように、非可換場の理論の計算ではダイアグラムを
2重線にするという手続きを取る。これは、open stringとのアナロジーを予感さ
せるものである。

弦理論でのopen stringの1-loop(図\ref{open-loop})のダイアグラムのUV領域は、
モジュラー変換によってclosed stringの伝播(図\ref{closed-loop})と結びつい
ているが、$g^{\mu\nu}$をclosed string のメトリックとしてclosed stringの
伝播は、
\begin{equation}
 \frac{1}{p_\mu\theta^{\mu\nu}p_\nu}
\end{equation}
という特異性を持っている。

非可換空間の上の場をopen stringと思った時に、図\ref{open-loop}のような
open stringの1-loopに対応する計算は非可換$\phi^3$理論の1-loopのnonplanar
な2点関数に対応する。このclosed stringの伝播の特異性は
\begin{equation}
 \label{nc-field-closed}
 g_{\mu\nu}\sim-(\theta^2)_{\mu\nu}
\end{equation}
とした時に、式(\ref{phi3-eff})の有効作用にある特異性と一致している。この
関係は、後に\ref{low-energy}節で求めるSeiberg Witten極限(\ref{SW-limit})
でのopen stringのメトリックとclosed stringのメトリックの関係式
(\ref{SW-rel})を求めるが、そこでopen stringのメトリックを$\delta^{ij}$と
した時のclosed stringのメトリックに一致する。

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\subsection{Seiberg-Witten 写像}
\label{Seiberg-Witten-Map}

$\theta$が一定な非可換$U(N)$ゲージ理論は実は、場の再定義で可換な空間の上
の$U(N)$ゲージ理論になることが知られている
\cite{STandNCG}\cite{ComGaugeEquivNCG}。その理由の説明は、弦理論の枠組の
中で説明されるがここではそのような場の再定義が存在するとして、場の再定義
を求めてみる。

もし、非可換ゲージ理論と可換ゲージ理論の間に場の再定義で結ばれた関係があっ
たとすれば、その場の再定義は以下のような図式を可換にしなくてはならない。
\begin{equation}
 \label{seiberg-witten-diagram}
 \xymatrix{
  \Ah (A)\ar[r]^{\delh_{\lamh}}&\Ah (A+\delta_\lambda A)\\
  A\ar[u]\ar[r]^{\delta_\lambda}&A+\delta_\lambda A\ar[u]}
\end{equation}
ただし、$\Ah,\delh,\lamh$はそれぞれ、非可換性$\theh$を入れた非可換ゲージ
理論のゲージ場,ゲージ変換,ゲージ変換のパラメータである。すなわち、場の再
定義とゲージ変換(式(\ref{nc-gauge-transf}))は可換でなくてはならない。さ
らにこの関係は一般に異なる$\theta$の非可換ゲージ理論がお互いに場の再定義
で結びついていることをしめしている。このとき、図式
(\ref{seiberg-witten-diagram})に対応するものを式で書き現わすと次のように
なる。
\begin{equation}
 \Ah(\At)+\delh_\lamh\Ah(\At)=\Ah(\At+\delt_\lamt)
\end{equation}
特に、
\begin{equation}
  \thet=\theh+\delta\theta \Longrightarrow
   \left\{\begin{array}{l}
    \Ah=\At+\delta\At(\At)+\Ord(\delta\theta^2) \\
    \lamh=\lamt+\delta\lamt(\lamt)+\Ord(\delta\theta^2) \\
   \end{array}\right.
\end{equation}
とすると上式は、次のようになる。
\begin{equation}
 \delh_\lamh\delta\At_i-\Dt_i\delta\lamt+i[\delta \Ah_i,]
  =-\1half\delta\theta^{kl}\{\partial_k\At_i,\partial_l\lamt\}
\end{equation}
この式は
\begin{subequations}
 \begin{eqnarray}
  \delta\Ah_i&=&-{1\over 4}\delta\theta^{kl}\{\At_k,\partial_l\At_i+
   \Ft\}+\alpha\delta\theta^{kl}\Dt_i\Ft_{kl}+\beta\delta\theta^{kl}
   \Dt_i[\At_k,\At_l] \\
  \delta\lamt&=&{1\over 4}\delta\theta^{kl}\{\partial_k\lamt,\At_l\}
   +2\beta\delta\theta^{kl}[\partial_k\lamt,\At_l] \\
  \delta\Ft&=&{1\over 4}\delta\theta^{kl}
   \left(2\{\Ft_{ik},\Ft_{jl}\}-\{\At_k,\Dt_l\Ft_{ij}
    +\partial_l\Ft_{ij}\}\right) \nonumber \\
  &&-i\alpha\delta^{kl}[\Ft_{ij},\Ft_{kl}]
   -i\beta\delta\theta^{kl}[\Ft_ij,[\At_k,\At_l]]
 \end{eqnarray}
\end{subequations}
によって解くことができる。ここで面白いのは、$\alpha,\beta$というパラメー
タの存在である。このパラメータの存在自体は実は不思議ではない。何故なら、
ひとつの微分方程式を解く為に3つの関数を使っているからである。
$\alpha,\beta$に比例する項はゲージ変換の形をしている。したがって、この
$\alpha,\beta$による不定性はゲージ変換で吸収することができ、物理的には意
味のないものである。

これで微小量の$\theta$の変化については解が求まったが、この解が有限の
$\theta$の変化についてうまく定義できているか、即ち可積分であるかどうかは
自明ではない。計算をすると実は、可積分ではないことが分かる。そのことを確
かめる為には二つの$\theta$の微小変化$\delta\theta_1,\delta\theta_2$の交
換子$[\delta_1]$が消えることを言わなくてはならない。この交換子の表式は非
常に長いので具体的な形は書かないが(具体的な形は\cite[式
(7)]{ComGaugeEquivNCG}を参照。)、大体次のような形をしている。
\begin{equation}
 [\delta_1,\delta_2]\At_i=\delta\theta_1\delta\theta_2(\cdots)
  +\Dt_i(\delta\theta_1\delta\theta_2\cdots)
  +\mbox{$\alpha,\beta$依存項}
\end{equation}
$\alpha,\beta$依存項はまたゲージ変換の形をしているので物理的ではないが、
それ以外は消えない。これは$\theta$の空間の中の閉回路を回って戻ってくると
もとのゲージ軌道と異なる軌道に行ってしまうことを示している。
